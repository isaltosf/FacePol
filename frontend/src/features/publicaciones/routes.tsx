import type { RouteObject } from 'react-router-dom'

/**
 * Rutas del módulo Publicaciones.
 *
 * TODO (Victor): agrega aquí las rutas de tu módulo (por ejemplo el feed que ya
 * aparece en el menú). App.tsx ya concatena esta lista, así que no necesitas
 * tocar ningún archivo fuera de esta carpeta.
 *
 * Ejemplo:
 *   { path: 'feed', element: <Feed /> }
 */
const rutas: RouteObject[] = []

export default rutas
