interface Props {
  comunidadId: number
}

/**
 * Punto de extensión del módulo Publicaciones dentro del detalle de una comunidad.
 *
 * TODO (Victor): reemplaza este stub por tus botones y enlaces (crear anuncio o
 * evento, listar las publicaciones de la comunidad...). La página de detalle de
 * comunidad ya lo renderiza pasándote el id, así que puedes trabajar solo en
 * este archivo sin tocar los de comunidades.
 */
export default function AccionesPublicaciones({ comunidadId }: Props) {
  void comunidadId

  return null
}
