interface Props {
  comunidadId: number
}

/**
 * Punto de extensión del módulo Membresías dentro del detalle de una comunidad.
 *
 * TODO (José): reemplaza este stub por tus botones y enlaces (solicitar unirse,
 * ver miembros, ver solicitudes pendientes...). La página de detalle de
 * comunidad ya lo renderiza pasándote el id, así que puedes trabajar solo en
 * este archivo sin tocar los de comunidades.
 */
export default function AccionesMembresia({ comunidadId }: Props) {
  void comunidadId

  return null
}
